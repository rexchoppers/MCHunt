version: '3'

services:
  mc-server:
    image: 'itzg/minecraft-server:2021.15.0'
    restart: always
    environment:
      EULA: "TRUE"
      VERSION: "1.15"
      TYPE: "SPIGOT"
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    volumes:
      - mc-server-data:/data

volumes:
  mc-server-data: {}